- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    ~/.aliases: shell/aliases
    ~/.functions: shell/functions
    ~/.oh-my-zsh: oh-my-zsh         
    ~/.vim_runtime: vim_runtime
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.tmux.conf: tmux
